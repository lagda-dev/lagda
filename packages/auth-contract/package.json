{
  "name": "@lagda/auth-contract",
  "version": "0.1.0",
  "description": "Lagda shared token, claims, and permission types for the resource server and auth service",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/auth-contract.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@lagda/core": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
