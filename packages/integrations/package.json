{
  "name": "@lagda/integrations",
  "version": "0.1.0",
  "description": "Lagda directory + signature-deploy integrations (Google Workspace, Microsoft stub)",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/integrations.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@lagda/core": "workspace:*",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
