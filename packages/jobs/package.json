{
  "name": "@lagda/jobs",
  "version": "0.1.0",
  "description": "Lagda background jobs (pg-boss) behind a Queue interface",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/jobs.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@lagda/core": "workspace:*",
    "pg-boss": "^10.1.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.6.0",
    "vitest": "^1.6.0"
  }
}
