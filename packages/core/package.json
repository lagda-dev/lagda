{
  "name": "@lagda/core",
  "version": "0.1.0",
  "description": "Lagda shared domain types, config schema, and utilities",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/core.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "oxlint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.6.0",
    "vitest": "^1.6.0"
  }
}
